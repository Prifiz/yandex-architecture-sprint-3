apiVersion: v1
kind: Service
metadata:
  name: {{ include "telemetry-manager-service.fullname" . }}
  labels:
    {{- include "telemetry-manager-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "telemetry-manager-service.selectorLabels" . | nindent 4 }}
