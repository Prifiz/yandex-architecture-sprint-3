apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "device-manager-service.fullname" . }}-test-connection"
  labels:
    {{- include "device-manager-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "device-manager-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
